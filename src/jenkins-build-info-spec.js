'use strict'

/* global describe, it */
describe('jenkins-build-info', () => {
  const jenkinsBuildInfo = require('.')
  it('write this test', () => {
    console.assert(jenkinsBuildInfo, 'should export something')
  })
})
